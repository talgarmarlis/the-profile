import { Divider} from "antd";
import {DividerBlock} from "../interface/block";

const NDivider: React.FC<{ block?: DividerBlock | any }> = ({ }) => {
    return <Divider />;
};

export default NDivider